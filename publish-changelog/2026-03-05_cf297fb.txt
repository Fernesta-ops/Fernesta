Publish Change Log
==================

Commit: cf297fbd89f1ae38c8ecc7a137606ce3449b19d6
Short Commit: cf297fb
Date: 2026-03-05T00:46:20+05:30
Author: Tarun Gupta <info@fernesta.com>
Message: Improve conversion UX, harden leads API, and optimize media delivery

Changed Files (Name/Status)
---------------------------
M	eslint.config.js
M	functions/api/lead.ts
M	package.json
M	public/downloads/fernesta-capabilities-deck.pdf
A	public/images/pages/about/hero/background-opt.jpg
A	public/images/pages/about/section/team-visual-opt.jpg
A	public/images/pages/clientele/hero/background-opt.jpg
A	public/images/pages/clientele/section/reporting-dashboard-opt.jpg
A	public/images/pages/contact/section/strategy-session-opt.jpg
A	public/images/pages/home/hero/visual-opt.jpg
A	public/images/pages/home/services/branding-opt.jpg
A	public/images/pages/home/services/performance-opt.jpg
A	public/images/pages/home/services/seo-opt.jpg
A	public/images/pages/home/services/web-conversion-opt.jpg
A	public/images/pages/services/hero/background-opt.jpg
A	scripts/generate-capabilities-deck.mjs
M	src/App.css
M	src/App.tsx
M	src/Components/CaseStudyShowcase.tsx
M	src/Components/ConversionDock.tsx
M	src/Components/Navbar.tsx
M	src/Components/Reveal.tsx
M	src/Components/StrategyAuditWidget.tsx
M	src/Components/TestimonialsSlider.tsx
M	src/pages/AboutPage.tsx
M	src/pages/CaseStudiesPage.tsx
M	src/pages/ClientelePage.tsx
M	src/pages/ContactPage.tsx
M	src/pages/HomePage.tsx

Full Diff
---------
commit cf297fbd89f1ae38c8ecc7a137606ce3449b19d6
Author: Tarun Gupta <info@fernesta.com>
Date:   Thu Mar 5 00:46:20 2026 +0530

    Improve conversion UX, harden leads API, and optimize media delivery

diff --git a/eslint.config.js b/eslint.config.js
index 5e6b472..763018d 100644
--- a/eslint.config.js
+++ b/eslint.config.js
@@ -6,7 +6,7 @@ import tseslint from 'typescript-eslint'
 import { defineConfig, globalIgnores } from 'eslint/config'
 
 export default defineConfig([
-  globalIgnores(['dist']),
+  globalIgnores(['dist', 'master/**', '.tmp-skill-scout/**', '.claude/**']),
   {
     files: ['**/*.{ts,tsx}'],
     extends: [
diff --git a/functions/api/lead.ts b/functions/api/lead.ts
index 3f640fc..e3adfcc 100644
--- a/functions/api/lead.ts
+++ b/functions/api/lead.ts
@@ -11,6 +11,14 @@ type LeadPayload = {
 };
 
 const ALLOWED_ORIGINS = ["https://fernesta.com", "https://www.fernesta.com"];
+const MAX_BODY_BYTES = 20_000;
+const MAX_FIELD_COUNT = 20;
+const MAX_KEY_LENGTH = 64;
+const MAX_VALUE_LENGTH = 1000;
+const RATE_WINDOW_MS = 5 * 60 * 1000;
+const RATE_MAX_REQUESTS = 6;
+const HONEYPOT_KEYS = new Set(["website", "url", "homepage", "hp_field"]);
+const rateStore = new Map<string, number[]>();
 
 function json(body: unknown, status = 200, origin = "") {
   const allowOrigin = ALLOWED_ORIGINS.includes(origin) ? origin : ALLOWED_ORIGINS[0];
@@ -35,6 +43,59 @@ function escapeHtml(value: string) {
     .replace(/'/g, "&#39;");
 }
 
+function isAllowedOrigin(origin: string) {
+  return !origin || ALLOWED_ORIGINS.includes(origin);
+}
+
+function getClientIp(request: Request) {
+  return (
+    request.headers.get("CF-Connecting-IP") ||
+    request.headers.get("X-Forwarded-For")?.split(",")[0]?.trim() ||
+    "unknown"
+  );
+}
+
+function cleanupRateStore(now = Date.now()) {
+  for (const [key, timestamps] of rateStore) {
+    const filtered = timestamps.filter((ts) => now - ts < RATE_WINDOW_MS);
+    if (!filtered.length) {
+      rateStore.delete(key);
+      continue;
+    }
+    rateStore.set(key, filtered);
+  }
+}
+
+function isRateLimited(key: string, now = Date.now()) {
+  cleanupRateStore(now);
+  const existing = rateStore.get(key) ?? [];
+  if (existing.length >= RATE_MAX_REQUESTS) return true;
+  existing.push(now);
+  rateStore.set(key, existing);
+  return false;
+}
+
+function normalizeFields(fields: unknown): Record<string, string> | null {
+  if (!fields || typeof fields !== "object" || Array.isArray(fields)) return null;
+  const entries = Object.entries(fields);
+  if (!entries.length || entries.length > MAX_FIELD_COUNT) return null;
+
+  const normalized: Record<string, string> = {};
+  for (const [rawKey, rawValue] of entries) {
+    const key = String(rawKey).trim().toLowerCase().replace(/\s+/g, "_");
+    if (!key || key.length > MAX_KEY_LENGTH) return null;
+    const value = String(rawValue ?? "").trim();
+    if (!value && !HONEYPOT_KEYS.has(key)) return null;
+    if (value.length > MAX_VALUE_LENGTH) return null;
+    normalized[key] = value;
+  }
+  return normalized;
+}
+
+function hasTriggeredHoneypot(fields: Record<string, string>) {
+  return Object.entries(fields).some(([key, value]) => HONEYPOT_KEYS.has(key) && Boolean(value.trim()));
+}
+
 function buildHtml(formName: string, fields: Record<string, string>) {
   const rows = Object.entries(fields)
     .map(
@@ -58,27 +119,76 @@ function buildHtml(formName: string, fields: Record<string, string>) {
 
 export const onRequestOptions = async (context: { request: Request }) => {
   const origin = context.request.headers.get("Origin") || "";
+  if (!isAllowedOrigin(origin)) {
+    return json({ error: "Origin not allowed." }, 403, origin);
+  }
   return json({ ok: true }, 200, origin);
 };
 
 export const onRequestPost: PagesFunction<Env> = async (context) => {
   const origin = context.request.headers.get("Origin") || "";
+  if (!isAllowedOrigin(origin)) {
+    return json({ error: "Origin not allowed." }, 403, origin);
+  }
+
+  const contentType = context.request.headers.get("Content-Type") || "";
+  if (!contentType.toLowerCase().includes("application/json")) {
+    return json({ error: "Unsupported content type." }, 415, origin);
+  }
+
+  const contentLength = Number(context.request.headers.get("Content-Length") || "0");
+  if (contentLength > MAX_BODY_BYTES) {
+    return json({ error: "Payload too large." }, 413, origin);
+  }
+
   const { RESEND_API_KEY, LEAD_FROM_EMAIL, LEAD_TO_EMAIL } = context.env;
   if (!RESEND_API_KEY || !LEAD_FROM_EMAIL || !LEAD_TO_EMAIL) {
     return json({ error: "Lead email provider not configured." }, 500, origin);
   }
 
-  const payload = (await context.request.json().catch(() => ({}))) as LeadPayload;
-  const subject = payload.subject?.trim() || "New Lead";
-  const formName = payload.formName?.trim() || "Website Form";
-  const fields = payload.fields || {};
+  const rawBody = await context.request.text().catch(() => "");
+  if (!rawBody || rawBody.length > MAX_BODY_BYTES) {
+    return json({ error: "Payload too large." }, 413, origin);
+  }
 
-  if (!Object.keys(fields).length) {
+  let payload: LeadPayload;
+  try {
+    payload = JSON.parse(rawBody || "{}") as LeadPayload;
+  } catch {
+    return json({ error: "Invalid JSON payload." }, 400, origin);
+  }
+  const subject = (payload.subject?.trim() || "New Lead").slice(0, 140);
+  const formName = (payload.formName?.trim() || "Website Form").slice(0, 120);
+  const fields = normalizeFields(payload.fields);
+  if (!fields) {
     return json({ error: "Invalid payload" }, 400, origin);
   }
 
-  const html = buildHtml(formName, fields);
-  const text = [`Form: ${formName}`, ...Object.entries(fields).map(([k, v]) => `${k}: ${v}`)].join("\n");
+  if (hasTriggeredHoneypot(fields)) {
+    return json({ success: true }, 200, origin);
+  }
+
+  const ip = getClientIp(context.request);
+  const rateKey = `${ip}:${formName.toLowerCase()}`;
+  if (isRateLimited(rateKey)) {
+    return json({ error: "Too many requests. Please try again later." }, 429, origin);
+  }
+
+  const cleanFields = Object.fromEntries(
+    Object.entries(fields).filter(([key]) => !HONEYPOT_KEYS.has(key))
+  );
+
+  if (!Object.keys(cleanFields).length) {
+    return json({ error: "Invalid payload" }, 400, origin);
+  }
+
+  const emailField = cleanFields.email;
+  if (emailField && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(emailField)) {
+    return json({ error: "Invalid email value." }, 400, origin);
+  }
+
+  const html = buildHtml(formName, cleanFields);
+  const text = [`Form: ${formName}`, ...Object.entries(cleanFields).map(([k, v]) => `${k}: ${v}`)].join("\n");
 
   const resendResponse = await fetch("https://api.resend.com/emails", {
     method: "POST",
diff --git a/package.json b/package.json
index 9420504..e592041 100644
--- a/package.json
+++ b/package.json
@@ -8,6 +8,7 @@
     "build": "tsc -b && vite build",
     "lint": "eslint .",
     "preview": "vite preview",
+    "generate:capabilities-deck": "node scripts/generate-capabilities-deck.mjs",
     "generate:case-study-pdfs": "node scripts/generate-case-study-pdfs-branded.mjs",
     "generate:case-study-pdfs:text": "node scripts/generate-case-study-pdfs.mjs"
   },
